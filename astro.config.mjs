import { defineConfig } from 'astro/config';

// ⚠️ Remplacer par le domaine définitif dès qu'il est acquis
// (utilisé pour sitemap.xml, robots.txt et les balises Open Graph).
export default defineConfig({
  site: 'https://centre-rahma.pages.dev',
});
